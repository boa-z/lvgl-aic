# MPP image decoder

The MPP image decoder is intentionally not implemented in Phase 1.

The later port must document decoder callbacks, draw-buffer ownership, cache
maintenance, and software-renderer fallback.
